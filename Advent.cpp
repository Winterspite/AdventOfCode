#include "Advent.h"

FileReader::FileReader(const char* filename)
{


}


// Reads all of the lines from the input file and provides that back as a std::vector
std::vector FileReader::Read()
{
  std::vector lines;

  return lines;
}
